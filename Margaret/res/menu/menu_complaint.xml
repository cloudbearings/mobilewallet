<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".HomeActivity" >

    <item
        android:id="@+id/action_new_complaint"
        android:icon="@drawable/ic_action_new"
        android:showAsAction="always"
        android:title="New Complaint"/>
    <item
        android:id="@+id/menu_more"
        android:icon="@drawable/ic_action_popup_more"
        android:orderInCategory="100"
        android:showAsAction="always"
        android:title="">
        <menu>
            <item
                android:id="@+id/verifiedOnly"
                android:showAsAction="never"
                android:title="Verified Only"/>
            <item
                android:id="@+id/newComplaintsOnly"
                android:showAsAction="never"
                android:title="New Only"/>
            <item
                android:id="@+id/paidComplaintsOnly"
                android:showAsAction="never"
                android:title="Paid Complaints"/>
            <item
                android:id="@+id/action_search"
                android:icon="@drawable/ic_action_search"
                android:showAsAction="never"
                android:title="Search"
                android:visible="false"/>
        </menu>
    </item>

</menu>